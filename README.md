# Cpu Frequency Scanner

Parse /proc/cpuinfo to get the current cpu frequencies of all your cores. Echos the mean of all
frequencies. This can be used in a status bar like polybar.

## Build

cargo build --relase 


